const name = 'João';
console.log(name);

const firstNumber = 10;
const secondNumber = 5;

const addition =  firstNumber + secondNumber;

console.log(addition);

const duplicateResult = addition * 2;

console.log(duplicateResult);

// typeof descobrir o tipo de uma variável

console.log(typeof(name), typeof(duplicateResult));